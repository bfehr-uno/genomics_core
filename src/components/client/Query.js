import { Container, TextField, Button } from '@material-ui/core';

const Query = () => (
  (
    <>
      <Container maxWidth="sm">
        <TextField />
        <Button />
      </Container>
    </>
  )
);

export default Query;
